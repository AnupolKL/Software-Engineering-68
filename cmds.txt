รัน 2 Terminal

# Terminal 1
python manage.py runserver

# Terminal 2
npm run tw:dev


.venv\scripts\activate

ดู pip install ที่ติดตั้งไปแล้ว ใช้ pip list

บันทึกข้อมูล database ทั้งหมดเก็บไว้ใน data.json
py manage.py dumpdata --indent 2 > data.json

# รันไฟล์นี้เพื่อ encode ไฟล์ข้อมูลที่เพี้ยน
python convert_to_utf8.py

# ย้อนไปที่เวอร์ชั่นที่ commit ก่อนหน้า
git reset --hard
